<% @project.each do |project| %>
  <h2><%= link_to project.title, project %></h2>
  <p>
    Published at <%= project.created_at.strftime('%b %d, %Y') %>
		by<%= project.user.email %>
  </p>
<% end %>
